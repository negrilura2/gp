# 缺失图表生成命令

当前环境未检测到可用的 Python 运行时，因此以下命令未在本轮直接执行。  
等本地 Python / Conda 环境可用后，可在项目根目录依次运行。

## 1. 生成 MFCC 特征图

```bash
python -m scripts.analysis.plot_mfcc --out thesis_materials/02_figures_existing/chapter2_feature/mfcc_feature_map.png
```

用途：

- 第2章 声纹特征提取技术

## 2. 生成 score normalization 评估结果

```bash
python -m scripts.evaluate --config configs/evaluate.yaml --score_norm none
python -m scripts.evaluate --config configs/evaluate.yaml --score_norm znorm
python -m scripts.evaluate --config configs/evaluate.yaml --score_norm tnorm
python -m scripts.evaluate --config configs/evaluate.yaml --score_norm snorm
```

预期补齐：

- `reports/backend_responses/*`
- ROC、EER、DET、minDCF、score distribution、calibration 等结果

## 3. 生成嵌入可视化图

```bash
python -m scripts.analysis.plot_embedding --config configs/plot_embedding.yaml --method tsne --score_norm none
python -m scripts.analysis.plot_embedding --config configs/plot_embedding.yaml --method pca --score_norm none
```

用途：

- 第5章 嵌入分布展示
- 第6章 可视化辅助分析

## 4. 生成噪声鲁棒性图

```bash
python -m scripts.analysis.noise_robustness --config configs/noise_robustness.yaml --model_path checkpoints/ecapa_mfcc_delta_best.pth
python -m scripts.analysis.noise_robustness --config configs/noise_robustness.yaml --model_path checkpoints/noise_augmented/ecapa_mfcc_delta_best.pth
```

用途：

- 第6章 不同噪声环境鲁棒性分析

## 5. 汇总实验结果

```bash
python -m scripts.analysis.summarize_experiments --config configs/summarize_experiments.yaml
```

用途：

- 生成实验对比结论
- 检查图表与指标是否完整

## 6. 生成延迟曲线

项目里目前没有稳定的独立脚本文件，建议后续补一个小脚本，从 `VerifyLog.latency_ms` 中导出：

- 横轴：样本序号或时间
- 纵轴：延迟毫秒数
- 输出：`reports/plots/latency/latency_curve.png`

用途：

- 第6章 系统延迟测试与分析
